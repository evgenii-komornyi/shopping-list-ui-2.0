import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
    card: {
        height: '450px',
        textAlign: 'center',
    },
    contentContainer: {
        height: '88%',
    },
    cardActions: {
        justifyContent: 'center',
    },
}));
